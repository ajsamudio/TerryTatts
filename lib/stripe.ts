/**
 * Stripe server client — DEMO MODE: not instantiated.
 *
 * GOING LIVE:
 *   1. npm install stripe
 *   2. Create a Stripe account, copy the secret key into STRIPE_SECRET_KEY
 *   3. Set up a webhook endpoint at /api/stripe/webhook and put the signing secret into STRIPE_WEBHOOK_SECRET
 *   4. Uncomment below
 */

// import Stripe from "stripe";
// export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
//   apiVersion: "2024-12-18.acacia",
// });

export {};
