/**
 * Resend client — DEMO MODE: not instantiated.
 *
 * GOING LIVE:
 *   1. npm install resend @react-email/components
 *   2. Create a sender domain at https://resend.com and set RESEND_API_KEY
 *   3. Uncomment below
 *   4. (Optional) create an audience and set RESEND_AUDIENCE_ID for newsletter signup
 */

// import { Resend } from "resend";
// export const resend = new Resend(process.env.RESEND_API_KEY!);

export {};
